# Asset Disposition

The authoritative legacy Harness repository was unavailable. Therefore no legacy file was silently preserved, deleted, or claimed compatible. This repository is a clean implementation of the locked v1.2.1 architecture. A future baseline reconciliation must classify every live asset as KEEP, PORT, ARCHIVE, or DELETE and record provenance.
